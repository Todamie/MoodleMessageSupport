<?php
defined('MOODLE_INTERNAL') || die();

function local_messagesupport_extend_navigation(global_navigation $nav) {
    /*$node = $nav->add(
        get_string('support', 'local_messagesupport'),
        new moodle_url('/local/messagesupport/index.php'),
        navigation_node::TYPE_CUSTOM,
        null,
        'messagesupport'
    );
    $node->showinflatnavigation = true; */
}